The Mod files are taken from /Examples/Mod of the project's root directory.

To build the examples, install the following packages (for Linux/x64):

libsdl2-dev
libsdl2-image-dev
libcurl4-openssl-dev
